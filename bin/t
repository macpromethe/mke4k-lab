#!/usr/bin/env bash
# Thin launcher — resolves the project root and delegates to t-commandline.bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "${SCRIPT_DIR}/t-commandline.bash" "$@"
